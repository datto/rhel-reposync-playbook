mirrors:
  children:
    dev:
      hosts:
        rhel-mirror-dev.example.net:
          ansible_ssh_user: cloud-user
    prod:
      hosts:
        rhel-mirror.example.net:
          ansible_ssh_user: cloud-user
